[package]
name = "nitpick-compress"
version = "0.11.2"
description = "Compression and decompression (gzip/deflate) for Nitpick"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/nitpick-packages"
categories = ["compression"]

[dependencies]

[build]
type = "library"
entry = "src/nitpick_compress.npk"
link_libraries = ["nitpick_compress_shim", "z"]
