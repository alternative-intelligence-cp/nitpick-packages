[package]
name = "nitpick-ringbuf"
version = "0.11.1"
authors = ["Nitpick Language Project"]
license = "MIT"
description = "Fixed-capacity circular buffer (ring buffer) with FIFO semantics"
keywords = ["ring-buffer", "circular-buffer", "queue", "data-structure"]
categories = ["data-structures"]

[build]
type = "library"
entry = "src/nitpick_ringbuf.npk"
