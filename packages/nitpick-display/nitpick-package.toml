[package]
name = "nitpick-display"
version = "0.12.2"
description = "Terminal display state manager — cursor, colors, attributes, dimensions"
license = "MIT"

[build]
type = "pure"

[dependencies]

[metadata]
categories = ["terminal", "display"]
