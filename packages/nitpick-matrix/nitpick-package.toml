[package]
name = "nitpick-matrix"
version = "0.11.1"
description = "Dense matrix operations — add, multiply, transpose, determinant, trace"
entry = "src/nitpick_matrix.npk"
type = "pure"
deps = ["nitpick-libc_mem"]
