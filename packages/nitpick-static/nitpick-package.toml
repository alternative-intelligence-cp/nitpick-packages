[package]
name = "nitpick-static"
version = "0.12.1"
description = "Static file path resolution and MIME type detection for Nitpick — pure Nitpick"
license = "MIT"
categories = ["networking", "middleware"]

[dependencies]
