[package]
name            = "nitpick-yaml"
version         = "0.2.6"
description     = "YAML configuration parser — load, query, and modify YAML key-value pairs with dotted-path access"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/nitpick-packages"
keywords        = ["yaml", "config", "parser", "serialization"]
categories      = ["data-formats", "parsing"]

[dependencies]

[build]
link_libraries = ["nitpick_yaml_shim"]
