[package]
name = "nitpick-resource-mem"
version = "0.2.0"
authors = ["Nitpick Language Project"]
license = "MIT"
description = "Consumable expiring memory cells — configurable read/write/TTL limits with Result-style access"
keywords = ["memory", "resource", "expiry", "constraint", "result"]
categories = ["data-structures", "memory"]

[build]
type = "library"
entry = "src/nitpick_resource_mem.npk"
