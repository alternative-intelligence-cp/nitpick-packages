[package]
name            = "nitpick-postgres"
version         = "0.2.3"
description     = "PostgreSQL database client for Nitpick — connect, query, parameterized queries, transactions via libpq"
license         = "MIT"
repository      = "https://github.com/alternative-intelligence-cp/nitpick-packages"

[dependencies]

[build]
link_libraries = ["pq"]
