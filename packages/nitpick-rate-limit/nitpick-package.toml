[package]
name = "nitpick-rate-limit"
version = "0.12.1"
description = "Token-bucket rate limiter for Nitpick — pure Nitpick"
license = "MIT"
categories = ["networking", "middleware"]

[dependencies]
