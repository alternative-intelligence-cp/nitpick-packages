[package]
name = "nitpick-socket"
version = "0.12.0"
description = "TCP and UDP socket abstractions for Nitpick — backed by aria-libc_net"
license = "MIT"
categories = ["networking"]

[dependencies]
aria-libc = "*"
link_libraries = ["aria_libc_net"]
