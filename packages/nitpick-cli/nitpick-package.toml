[package]
name = "nitpick-cli"
version = "0.11.2"
description = "Rich CLI output with ANSI colors, styles, and progress bars for Nitpick"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/aria-packages"
categories = ["cli", "terminal"]

[dependencies]

[build]
type = "library"
entry = "src/nitpick_cli.npk"
link_libraries = ["nitpick_cli_shim"]
