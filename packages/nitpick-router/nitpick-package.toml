[package]
name = "nitpick-router"
version = "0.12.0"
description = "Express-style HTTP router for Nitpick — route matching, path params, middleware"
license = "MIT"
categories = ["networking", "routing"]

[dependencies]
nitpick-libc = "*"
nitpick-server = "*"
