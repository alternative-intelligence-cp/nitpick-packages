use "../src/tensor.npk".*;
func:main = int32() {
    flt32->:x = alloc(32i64) => flt32->;
    flt32->:x_slice = x[8i64..16i64];
    x[8i64] = 42.0f32;
    if (x_slice[0i64] != 42.0f32) {
        exit(1i32);
    }
    exit(0i32);
};
