[package]
name = "nitpick-result"
version = "0.11.3"
description = "Extended Result/Option combinators (ok, err, unwrap, unwrap_or, and, or)"
license = "MIT"
repository = "https://github.com/alternative-intelligence-cp/aria-packages"
categories = ["data-structures", "error-handling"]

[dependencies]
aria-libc = "0.10.0"

[build]
type = "library"
entry = "src/nitpick_result.npk"
link_libraries = ["aria_libc_mem"]
