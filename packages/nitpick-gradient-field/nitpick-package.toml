[package]
name = "nitpick-gradient-field"
version = "0.2.0"
description = "GradientField — spatial decision field driven by a single DGT emitter (LIB-27)"
author = "Randy Hoggard"
license = "MIT"

[dependencies]
aria-decision-t = { path = "../aria-decision-t" }
