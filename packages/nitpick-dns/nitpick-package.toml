[package]
name = "nitpick-dns"
version = "0.12.0"
description = "DNS resolution for Nitpick — hostname to IP address lookup via aria-libc_net"
license = "MIT"
categories = ["networking"]

[dependencies]
aria-libc = "*"
link_libraries = ["aria_libc_net"]
