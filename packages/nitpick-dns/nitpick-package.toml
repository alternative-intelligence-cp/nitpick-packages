[package]
name = "nitpick-dns"
version = "0.12.0"
description = "DNS resolution for Nitpick — hostname to IP address lookup via nitpick-libc_net"
license = "MIT"
categories = ["networking"]

[dependencies]
nitpick-libc = "*"
link_libraries = ["nitpick_libc_net"]
