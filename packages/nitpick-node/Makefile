build:
	mkdir -p lib
	/home/randy/Workspace/REPOS/nitpick/build/npkc src/core_logic.npk -c -o lib/core_logic.o -I ../nitpick-api/src -I ../nitpick-http/src
	npm install

clean:
	rm -rf lib build node_modules
