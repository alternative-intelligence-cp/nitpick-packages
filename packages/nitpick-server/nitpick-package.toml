[package]
name = "nitpick-server"
version = "0.12.0"
description = "HTTP/1.1 server for Nitpick — built on aria-socket and aria-libc_net"
license = "MIT"
categories = ["networking", "server"]

[dependencies]
aria-libc = "*"
aria-socket = "*"
link_libraries = ["aria_libc_net", "aria_libc_mem"]
