use "process.npk".*;
use "string.npk".*;

func:main = int32() {
    string:test = "hello";
    if (test == "") {
        exit 1;
    }
    exit 0;
};

func:failsafe = int32(tbb32:err) {
    exit 1;
};
