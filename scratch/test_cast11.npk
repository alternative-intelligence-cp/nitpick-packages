extern "libc" {
    func:malloc_f64 = wild float64*(int64:size) @alias("malloc");
    func:free = void(wild float64*:ptr);
}

pub func:failsafe = int32(tbb32:err) { exit 1; };

pub func:main = int32() {
    wild float64->:ptr = malloc_f64(10i64);
    ptr[0i64] = 42.0;
    free(ptr);
    exit 0;
};
