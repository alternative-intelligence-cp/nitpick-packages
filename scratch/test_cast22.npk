extern func:my_malloc = wild float64*(int64:size);
extern func:my_free = void(wild float64*:ptr);

pub func:failsafe = int32(tbb32:err) { exit 1; };

pub func:main = int32() {
    wild float64->:ptr = my_malloc(10i64);
    ptr[0i64] = 42.0;
    my_free(ptr);
    exit 0;
};
